{
  "installCommand": "npm cache clean --force && npm i vite -g && npm install",
  "buildCommand": "npm run build",
  "version": 2,
  "version": 2,
  "builds": [
    {
      "src": "dist/client/**/*",
      "use": "@vercel/static"
    },
    {
      "src": "api/ssr.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "dist/client/**",
          "dist/server/**"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/ssr.js"
    }
  ]
}
