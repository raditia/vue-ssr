{
  "version": 2,
  "installCommand": "npm cache clean --force && npm i vite -g && npm install",
  "buildCommand": "vite build --ssrManifest --outDir dist/client && cp index.html dist/client && vite build --ssr src/entry-server.js --outDir dist/server && ls -R dist",
  "version": 2,
  "builds": [
    {
      "src": "dist/client/**/*",
      "use": "@vercel/static"
    },
    {
      "src": "api/ssr.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "dist/client/**",
          "dist/server/**"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/ssr.js"
    }
  ]
}
