{
  "version": 2,
  "installCommand": "npm cache clean --force && npm i vite -g && npm install",
  "builds": [
    {
      "src": "api/ssr.js",
      "use": "@vercel/node"
    },
    {
      "src": "dist/client/**/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/ssr.js"
    }
  ]
}
